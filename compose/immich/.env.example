# Immich Configuration
# Copy to .env and customize, or let Ansible manage this file

# Timezone
TZ=Australia/Melbourne

# Database password (generate with: openssl rand -base64 32)
DB_PASSWORD=CHANGE_ME_SECURE_PASSWORD

# Upload location (where new uploads will be stored)
UPLOAD_LOCATION=/opt/immich/upload

# External library location (read-only, for importing existing photos)
# This should be your NFS mount from TrueNAS
EXTERNAL_LIBRARY=/mnt/photos
