# Immich Configuration

# Timezone
TZ=America/New_York

# Database password (generate with: openssl rand -base64 32)
DB_PASSWORD=CHANGE_ME_SECURE_PASSWORD

# Upload location (where photos will be stored)
UPLOAD_LOCATION=/opt/immich/upload

# OAuth/OIDC Configuration (configure after Authentik is set up)
OAUTH_ENABLED=false
OAUTH_ISSUER_URL=https://auth.home.lab/application/o/immich/
OAUTH_CLIENT_ID=immich
OAUTH_CLIENT_SECRET=CHANGE_ME_CLIENT_SECRET
